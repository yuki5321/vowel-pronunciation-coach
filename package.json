{
  "name": "vowel-pronunciation-coach",
  "version": "1.0.0",
  "description": "English vowel pronunciation practice application",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "echo \"No tests yet...\"",
    "start:api": "func start --cors http://localhost:3000",
    "dev": "concurrently \"npm run start\" \"npm run start:api\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "axios": "^1.10.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "^5.0.1",
    "recorder-js": "^1.0.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
